# AI Dashboard

Current Milestone: `/init` completed — AI folder and agents initialized.

Tech stack (brief):
- Backend: Node.js + Express-style layout in `backend/` (controllers, services, routes, SQL schema).
- Frontend: React + Vite SPA in `frontend/` (JSX components, services, and Vite config).

Active Assignments:
- Manager: verify and log `/init` (completed).
- Policy: Sub-agent autonomy enabled — runs must be logged; monitor token impact.

Token Efficiency Tracker (estimates):
- Init/files creation: ~300 tokens
- Per-task delegation (avg): 80-300 tokens depending on snippet size

Notes: Keep delegation payloads to file paths + ≤50-line snippets when possible.

Init artifacts: [AI/team_roster.md](AI/team_roster.md), [AI/dashboard.md](AI/dashboard.md), [AI/task_board.md](AI/task_board.md), [AI/changelog.md](AI/changelog.md), agent files in [AI/agents/](AI/agents/).
